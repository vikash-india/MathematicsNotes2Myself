#-------------------------------------------------------------------------
# Control various parts of the build
#-------------------------------------------------------------------------
# Clean before building
is.build.clean=false

#-------------------------------------------------------------------------
# Leave the following properties unchanged.
#-------------------------------------------------------------------------
# LaTeX equations source directory
latex.equations.src.dir=${basedir}/src/code/latex

# LaTeX equations target directory
latex.equations.target.dir=${latex.equations.src.dir}/images

# LaTeX numbers source directory
latex.numbers.src.dir=${basedir}/src/mathematics/numbers

# LaTeX numbersTarget directory
latex.numbers.target.dir=${latex.numbers.src.dir}/images
