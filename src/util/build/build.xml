<project name="MathematicsNotes2Myself"
         default="build"
         basedir="/home/vikash/Personal/Workspace/MathematicsNotes2Myself/">

    <description>Convert LaTeX code to images</description>

    <property file="${basedir}/src/util/build/build.properties"/>

    <target name="clean"
            depends="clean.latex, clean.numbers"
            description="Clean equations and numbers images."/>

    <target name="build"
            depends="build.latex, build.numbers"
            description="Convert LaTeX equations and numbers code to images."/>

    <target name="clean.latex"
            description="Clean equations images.">
        <delete>
            <fileset dir="${latex.equations.target.dir}" includes="**/*.png"/>
        </delete>
    </target>

    <target name="clean.numbers"
            description="Clean numbers images.">
        <delete>
            <fileset dir="${latex.numbers.target.dir}" includes="**/*.png"/>
        </delete>
    </target>

    <target name="build.latex"
            description="Convert LaTeX equations code to images.">
        <apply executable="tex2im" failonerror="true" dir="${latex.equations.src.dir}">
            <arg value="-o"/>
            <targetfile/>

            <fileset dir="${latex.equations.src.dir}">
                <include name="**/*.tex"/>
                <exclude name="**/*Template*"/>
            </fileset>
            <mapper type="regexp"
                    from="(.*)\.tex"
                    to="${latex.equations.target.dir}/\1.png"/>
        </apply>
    </target>

    <target name="build.numbers"
            description="Convert LaTeX numbers code to images.">
        <apply executable="tex2im" failonerror="true" dir="${latex.numbers.src.dir}">
            <arg value="-o"/>
            <targetfile/>

            <fileset dir="${latex.numbers.src.dir}">
                <include name="**/*.tex"/>
                <exclude name="**/*Template*"/>
            </fileset>
            <mapper type="regexp"
                    from="(.*)\.tex"
                    to="${latex.numbers.target.dir}/\1.png"/>
        </apply>
    </target>
</project>
